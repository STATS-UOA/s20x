Stage 19: review optional plotting-engine behaviour end-to-end.

Stage 19.1: manually test normcheck(), eovcheck(), modelcheck(), and pairs20x() with base and optional engines.

- Confirm base remains default.
- Confirm ggplot2/GGally engines work when installed.
- Confirm clean errors when optional packages are unavailable.
- Compare output against the original teaching plots.
- Fix only behaviour regressions or obvious polish issues.